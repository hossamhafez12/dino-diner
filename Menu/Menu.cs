/*using System;
using System.Collections.Generic;
using System.Text;
using Xunit;

namespace DinoDiner.Menu
{
    public class Menu
    {
        public List<IMenuItem> AvailableMenuItems
        {
            get
            {
                List<IMenuItem> availableMenuItems = new List<IMenuItem>();

                availableMenuItems.AddRange(Menu.AvailableEntrees);
                availableMenuItems.Add(Fryceritops);
                availableMenuItems.Add(MeteorMacAndCheese);
                availableMenuItems.Add(MezzorellaSticks);
                availableMenuItems.Add(Triceritots);
                availableMenuItems.Add(JurassicJava);
                availableMenuItems.Add(Sodasaurus);
                availableMenuItems.Add(Tyrannotea);
                availableMenuItems.Add(Water);

                return availableMenuItems;

            }
        }
        public List<IMenuItem> AvailableEntrees
        {
            get
            {
                List<IMenuItem> availableEntrees = new List<IMenuItem>();
                availableEntrees.Add(Brontowurst);
                availableEntrees.Add(DinoNuggets);
                availableEntrees.Add(PrehistoricPBJ);
                availableEntrees.Add(PterodactylWings);
                availableEntrees.Add(SteakosaurusBurger);
                availableEntrees.Add(TRexKingBurger);
                availableEntrees.Add(VelociWrap);

                return availableEntrees;
            }
        }
        
    }
}*/
